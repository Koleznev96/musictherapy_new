import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  item_button: {
    marginTop: 25,
    width: '100%',
    height: 150,
    borderRadius: 16,
  },
  item_name: {
    fontSize: 24,
  },
});
